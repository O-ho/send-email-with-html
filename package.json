{
  "name": "email-sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "send:secure": "node send-secure-mail.js",
    "preview:inner": "node send-secure-mail.js --preview-inner",
    "preview:email": "node send-secure-mail.js --preview-email-body",
    "preview:all": "node send-secure-mail.js --preview-all",
    "preview:dev": "node send-secure-mail.js --preview-all && concurrently -k -n build,web -c blue,magenta \"nodemon --legacy-watch --watch send-secure-mail.js --watch templates --ext js,html --exec \\\"node send-secure-mail.js --preview-all\\\"\" \"live-server dist --port=5173 --open=preview.html --wait=150\""
  },
  "dependencies": {
    "@react-email/components": "^1.0.12",
    "@react-email/render": "^2.0.6",
    "crypto-js": "^4.2.0",
    "dotenv": "^16.4.7",
    "nodemailer": "^6.9.16",
    "pdfkit": "^0.18.0",
    "puppeteer-core": "^24.40.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "live-server": "^1.2.2",
    "nodemon": "^3.1.9"
  },
  "private": true
}
